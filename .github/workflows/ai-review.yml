name: AI Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: AI Review
        run: |
          echo "AI review started..."
          echo "Code structure is good."
          echo "Design looks clean."
